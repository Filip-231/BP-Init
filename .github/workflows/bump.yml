name: Bump Version

on:
  workflow_dispatch:

jobs:
  bump:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install dependencies
        run: make install

      - name: Run check
        run: make bump

      - name: Bump Version
        run: make all
